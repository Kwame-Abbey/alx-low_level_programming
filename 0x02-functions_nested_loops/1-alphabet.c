#include "main.h"

/**
 * main - (Prints the alphabets in lowercase)
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a = 'a';

	while (a <= 'z')
	{
		putchar('a');
	}
	putchar('\n');
	return (0);
}
